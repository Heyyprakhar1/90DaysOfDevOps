kind: Pod
apiVersion: v1

metadata:
  name: pod-nginx
  namespace: app-ns

spec:
  containers:
    - name: nginx
      image: nginx:latest
      volumeMounts:
      - name: nginx-conf
        mountPath: /etc/nginx/conf.d

  volumes:
  - name: nginx-conf
    configMap:
      name: nginx-config
